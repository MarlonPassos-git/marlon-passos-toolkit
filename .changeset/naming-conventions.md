---
"@m-p-toolkit/biome-config": minor
---

Add base naming conventions for variables, parameters, functions, classes, types, global constants, enum members, private class members, and namespace imports.
