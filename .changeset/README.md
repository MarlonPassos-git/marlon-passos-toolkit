# Changesets

Run `pnpm changeset` when a change affects a published package in a user-visible way.

Use `pnpm changeset:version` only when preparing a version update, then publish with the existing GitHub Actions workflow or `pnpm changeset:publish` when a manual local publish is intentional.
