{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "inputs": ["**/*.ts", "**/*.js", "**/*.json", "biome.json"],
      "cache": true
    },
    "format": {
      "inputs": ["**/*.ts", "**/*.js", "**/*.json", "biome.json"],
      "cache": true
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
